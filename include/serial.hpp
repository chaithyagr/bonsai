template < class T>
void print_random_data(const char *string, T pos, int num, int n);
template < class T>
void print_upto_n(const char * string, T pos, int num);
int get_pots(int numPoints, float theta, float *pots, float *points, float *weights, bool dodebug);
